const authController = require("./authController")
const artController = require("./artController")
const subsController = require("./subsController")

module.exports = {
    authController, artController, subsController
}